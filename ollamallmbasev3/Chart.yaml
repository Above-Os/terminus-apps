apiVersion: v2
appVersion: 0.33.1
description: "Generic Ollama + llm-init base; the model is supplied at install time via env. Fixed multi-node startup by scheduling llm-init and the inference engine on the same node to share local run-state."
name: ollamallmbasev3
type: application
version: 1.3.24