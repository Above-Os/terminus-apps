# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   ollama          — engine Deployment
#   ollamallmbasev3 — llm-init / entrance Deployment
workloads:
  ollama:
    replicaCount: 1
  ollamallmbasev3:
    replicaCount: 1
