metadata:
  title: n8n
  description: n8n is an extendable workflow automation tool.

spec:
  fullDescription: |
    n8n is an extendable workflow automation tool. With a fair-code distribution model, n8n will always have visible source code, be available to self-host, and allow you to add your own custom functions, logic and apps. n8n's node-based approach makes it highly versatile, enabling you to connect anything to everything.

    **Available integrations**
    n8n has 200+ different nodes to automate workflows. The list can be found on: https://n8n.io/integrations

    **Build powerful workflows, really fast**
    - Handle really complex processes  
    - Simple debugging  
    - Use templates to get started fast  
    - Code when you need it, UI when you don't  
    - Reuse your work

    **Get peace of mind**
    - Connect to any system  
    - Customize error handling  
    - Horizontal scaling  
    - Host on your own infrastructure

    **Features**
    - Visual workflow editor
    - Run JavaScript
    - npm package import
    - Workflows can wait
    - Conditional logic and branching
    - Webhook triggers
    - Retry failed executions
    - Multi-trigger workflows
    - Source code available
    - User management
    - Create custom nodes
    - Encrypted credential store
    - Copy + paste workflows
    - Export workflows to JSON
    - Custom error workflows

  upgradeDescription: |
    Upgrade n8n to 2.40.7 (chart 1.1.8).

    **Deployment Changes**
    - Image: `n8nio/n8n:2.40.7`
    - HostPath, reverse-proxy, and timeout envs unchanged: `N8N_WEBHOOK_URL`, `N8N_PROTOCOL`, `N8N_EDITOR_BASE_URL`, `N8N_PROXY_HOPS`, `N8N_SECURE_COOKIE`, `N8N_RUNNERS_TASK_TIMEOUT=300`

    **Env / startup notes (2.33.0 → 2.40.7)**
    - No required startup-command changes (default image entrypoint)
    - No new required environment variables for this chart
    - Upstream 2.x rolling upgrade; database migrations run automatically on startup

    **Important Notes**
    - Review workflow compatibility using Settings > Migration Report after upgrade
    - npm latest / Docker Hub `latest` and `stable` currently resolve to 2.40.7

    See: https://github.com/n8n-io/n8n/releases/tag/n8n%402.40.7
