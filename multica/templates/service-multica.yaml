apiVersion: v1
kind: Service
metadata:
  name: multica
  namespace: {{ .Release.Namespace }}
  labels:
    app: multica
spec:
  type: ClusterIP
  selector:
    app: multica
  ports:
    - name: http
      port: 3000
      targetPort: 3000
