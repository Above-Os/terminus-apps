metadata:
  title: Jina CLIP v2
  description: Jina CLIP v2 embedding service (auto select OpenVINO iGPU / ONNX CPU).

spec:
  fullDescription: |
    **IMPORTANT NOTE**
    This is a shared app. Once installed by the Olares Admin, all users in the cluster can use it through reference app.

    **ENGINE AUTO SELECT**
    - Intel iGPU: OpenVINO IR backend
    - Others: ONNX Runtime (CPU) backend

    **MODEL REPOS**
    - OpenVINO: wangzhong/jina-clip-v2-split-openvino
    - ONNX: wangzhong/jina-clip-v2-split-onnx

    **API**
    - POST /api/text/embed
    - POST /api/image/embed
  upgradeDescription: |
    **Jina CLIP v2 1.0.10**

    - Marked as suspended via the empty `.suspend` marker file. The app is no longer distributed through Market while preserving the chart and manifest content.
