metadata:
  title: EmbeddingGemma
  description: "EmbeddingGemma-300M text embeddings via IREmbeddingServer (OpenVINO / ONNX)"

spec:
  upgradeDescription: |
    v1.1.4: Model Console (llm-init) updated to v1.7.21.

  fullDescription: |
    **EmbeddingGemma-300M** text embedding service powered by EmbeddingServer. Serves OpenAI-compatible `/v1/embeddings` and `/v1/models` on port 8080.

    **Accelerator modes**
    | Olares mode | Image | Backend | `ACCELERATOR` |
    |-------------|-------|---------|----------------|
    | `intel` | `embed-server:hw-bfaa3cc-ov-intel` | OpenVINO IR | `intel` |
    | `intel-gpu` | `embed-server:hw-bfaa3cc-ov-intel` | OpenVINO IR | `intel-gpu` |
    | `cpu` | `embed-server:hw-bfaa3cc-onnx-cpu` | ONNX CPU EP | `cpu` |
    | `nvidia` | `embed-server:hw-bfaa3cc-onnx-cuda12/13` | ONNX CUDA EP | `nvidia` |
    | `nvidia-gb10` | `embed-server:hw-bfaa3cc-onnx-cuda13-gb10-arm64` | ONNX CUDA EP | `nvidia-gb10` |

    **Model download**
    On first start, llm-init downloads from the unified Hugging Face repo into App Common cache:
    - [beclab/embeddinggemma-300m](https://huggingface.co/beclab/embeddinggemma-300m)
    - `intel` mode: `openvino/` subdirectory only
    - All other modes: `onnx/` subdirectory only

    **Device mounts**
    - `intel`: `/dev/dri`, `/sys/class/drm` for Intel iGPU stack
    - `nvidia` / `nvidia-gb10`: GPU injection via Olares scheduler

    **Stack**
    - **embed-server** (`beclab/embed-server:hw-bfaa3cc`, internal port 8080): Rust embedding inference.
    - **llm-init** (`beclab/llm-init:v1.3.5`, `ENGINE_KIND=embed`, port 8090): download, progress UI, and OpenAI API proxy.
