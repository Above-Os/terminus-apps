apiVersion: v1
kind: ServiceAccount
metadata:
  name: ytdlp-cluster-account
  namespace:  {{ .Release.Namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ytdlp-cluster-role
rules:
- apiGroups: 
    - '*'            
  resources: 
    - statefulsets
    - daemonsets
    - namespaces   
    - serviceaccounts/token
  verbs: 
    - '*'

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ytdlp-cluster-binding
subjects:
- kind: ServiceAccount
  name: ytdlp-cluster-account
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: ytdlp-cluster-role
  apiGroup: rbac.authorization.k8s.io

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ytdlp
  namespace: {{ .Release.Namespace }}
  labels:
    app: ytdlp
    applications.app.bytetrade.io/author: bytetrade.io
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ytdlp
  template:
    metadata:
      labels:
        app: ytdlp
        io.kompose.service: ytdlp
    spec:
      serviceAccountName: ytdlp-cluster-account
      serviceAccount: ytdlp-cluster-account
      securityContext:
        runAsUser: 0
        runAsNonRoot: false
      containers:
      - name: yt-dlp
        image: "beclab/ytdlp-server:v0.0.0.5.17.3"
        imagePullPolicy: IfNotPresent
        securityContext:
          runAsUser: 0
          runAsNonRoot: false
        ports:
        - containerPort: 3082
        env:
        - name: DEBUG
          value: "false"
        - name: FAIL_THRESHOLD
          value: "2"
        - name: YTDLP_PG_HOST
          value: "{{ .Values.postgres.host }}"
        - name: YTDLP_PG_PORT
          value: "{{ .Values.postgres.port }}"
        - name: YTDLP_PG_USERNAME
          value: "{{ .Values.postgres.username }}"
        - name: YTDLP_PG_PASSWORD
          value: "{{ .Values.postgres.password }}"
        - name: YTDLP_PG_DATABASE
          value: "{{ .Values.postgres.databases.ytdlp }}"

        volumeMounts:
        - name: config-dir
          mountPath: /app/config
        - name: download-dir
          mountPath: /app/downloads
        resources:
          requests:
            cpu: 20m
            memory: 50Mi
          limits:
            cpu: "1"
            memory: 300Mi

      volumes:
      - name: config-dir
        hostPath: 
          type: DirectoryOrCreate
          path: '{{ .Values.rootPath }}/userdata/Cache/download'
      - name: download-dir
        hostPath:
          type: DirectoryOrCreate
          path: '{{ .Values.rootPath }}/rootfs/userspace'
          
---

apiVersion: v1
kind: Service
metadata:
  name: ytdlp-svc
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: ytdlp
  ports:
    - name: ytdlp-server
      protocol: TCP
      port: 3082
      targetPort: 3082
      
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    io.kompose.service: ytdlp
  name: sharedentrances-ytdlp
  namespace: "{{ .Release.Namespace }}"
spec:
  ports:
    - name: "3082"
      port: 80
      targetPort: 3082
  selector:
    io.kompose.service: ytdlp
status:
  loadBalancer: {}