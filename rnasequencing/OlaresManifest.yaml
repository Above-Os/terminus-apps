olaresManifest.version: '0.10.0'
metadata:
  name: rnasequencing
  description: Single-cell RNA sequencing (scRNA-seq) lets researchers study gene activity in each cell on its own
  icon: https://app.cdn.olares.com/appstore/default/defaulticon.webp
  appid: rnasequencing
  version: '1.0.0'
  title: RNA Sequencing
  categories:
  - Productivity_v112
  - Utilities
permission:
  appData: true
  appCache: true
  userData:
  - Home
spec:
  versionName: '0.0.5'
  fullDescription: |
    Single-cell RNA sequencing (scRNA-seq) lets researchers study gene activity in each cell on its own,
    exposing variation, cell types, and cell states that bulk methods hide.
    But these large, high-dimensional datasets take heavy compute to handle.
    
    This playbook shows an end-to-end GPU-powered workflow for scRNA-seq using RAPIDS-singlecell,
    a RAPIDS powered library in the scverse® ecosystem.
    It follows the familiar Scanpy API and lets researchers run the steps of data preprocessing, quality control (QC) and cleanup,
    visualization, and investigation faster than CPU tools by working with sparse count matrices directly on the GPU.
  developer: NVIDIA
  website: https://www.nvidia.com/
  submitter: Olares
  locale:
  - en-US
  - zh-CN
  {{- if and .Values.admin .Values.bfl.username (eq .Values.admin .Values.bfl.username) }}
  requiredMemory: 20Gi
  limitedMemory: 40Gi
  requiredDisk: 6Gi
  limitedDisk: 15Gi
  requiredCpu: 1
  limitedCpu: 2
  requiredGpu: 10Gi
  limitedGpu: 24Gi
  {{- else }}
  requiredMemory: 20Gi
  limitedMemory: 40Gi
  requiredDisk: 6Gi
  limitedDisk: 15Gi
  requiredCpu: 1
  limitedCpu: 2
  {{- end }}
  doc: https://build.nvidia.com/spark/single-cell
  supportArch:
  - amd64
  - arm64
options:
  dependencies:
  - name: olares
    type: system
    version: '>=1.12.3-0'
entrances:
  - authLevel: internal
    host: rnasequencingingress
    icon: https://app.cdn.olares.com/appstore/default/defaulticon.webp
    name: rna
    openMethod: window
    port: 8080
    title: RNA Sequencing
olaresManifest.type: app
