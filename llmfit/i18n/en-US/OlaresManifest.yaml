metadata:
  title: LLMFit
  description: Web Dashboard for hardware-aware LLM recommendations; includes REST API.
spec:
  fullDescription: |
    **Primary use in Olares: Web Dashboard** — browse models in the browser with the same fit/scoring as the upstream CLI/TUI (hardware summary, scores, quantization, tok/s estimates, filters). The backend runs `llmfit serve`; the app entrance proxies it for a normal web experience.

    LLMFit right-sizes LLM models to your system's RAM, CPU, and GPU as seen by the workload (in containers, limits may differ from the full node unless devices/overrides are configured).

    # Features
    * Detects system RAM, CPU cores, and GPU (NVIDIA, AMD, Intel Arc, Apple Silicon, Ascend)
    * Multi-GPU support with aggregated VRAM
    * Scores models across 4 dimensions: Quality, Speed, Fit, Context
    * Dynamic quantization selection (Q8_0 through Q2_K)
    * MoE (Mixture-of-Experts) architecture aware
    * Speed estimation based on actual GPU memory bandwidth
    * Web Dashboard + REST API with filtering, sorting, and top-model selection
    * Supports Ollama, llama.cpp, MLX runtime provider detection
    * 200+ models from HuggingFace in database
    * Use-case filtering: General, Coding, Reasoning, Chat, Multimodal, Embedding
  upgradeDescription: |
    Web Dashboard–oriented: visible entrance, browser window launch, updated copy. See upstream releases: https://github.com/AlexsJones/llmfit/releases
