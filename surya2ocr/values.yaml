# Per-workload replica counts (keys match Deployment metadata.name /
# workloadReplicas in OlaresManifest).
#   surya2ocr   — llama.cpp CUDA engine (== appid / Release.Name)
#   llminit     — llm-init download + OCR API proxy
#   ocradapter  — OCRAdapter (Redis job queue → llamacpp)
workloads:
  llminit:
    replicaCount: 1
  ocradapter:
    replicaCount: 1
  surya2ocr:
    replicaCount: 1
