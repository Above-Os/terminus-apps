# Replica count per workload; the keys must match workloadReplicas in OlaresManifest.
workloads:
  audiovoicestudiov3:
    replicaCount: 1
# Platform values (userspace, olaresEnv, …) are injected at render.
