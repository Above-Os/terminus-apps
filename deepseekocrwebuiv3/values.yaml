# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
workloads:
  deepseekocrwebuiv3:
    replicaCount: 1
  deepseekocrwebuiv3-download:
    replicaCount: 1
  deepseekocrwebui-web:
    replicaCount: 1
