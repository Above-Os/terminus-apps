apiVersion: v2
appVersion: 0.5.18-cu130
description: "Generic SGLang + llm-init base; the model is supplied at install time via env. Fixed multi-node startup by scheduling llm-init and the inference engine on the same node to share local run-state."
name: sglangllmbasev3
type: application
version: 1.2.40