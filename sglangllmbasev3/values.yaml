# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas.deployments in OlaresManifest).
#   sglang          — engine Deployment
#   sglangllmbasev3 — llm-init / entrance Deployment
workloads:
  sglang:
    replicaCount: 1
  sglangllmbasev3:
    replicaCount: 1
