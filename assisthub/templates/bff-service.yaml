apiVersion: v1
kind: Service
metadata:
  name: assisthub-bff-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app: assisthub-bff
spec:
  selector:
    app: assisthub-bff
  ports:
    - protocol: TCP
      port: 4200
      targetPort: 4200
