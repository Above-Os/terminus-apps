apiVersion: v1
kind: Service
metadata:
  name: assisthub-web-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app: assisthub
spec:
  selector:
    app: assisthub
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
