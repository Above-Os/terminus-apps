apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (vLLM engine, llm-init audio-proxy variant). One instance = one model; capabilities selected via MODEL_SUPPORTS (stt / stt_stream / align). llm-init downloads the model AND fronts the engine's OpenAI-compatible /v1/* directly (ENGINE_KIND=audio, no nginx).
name: audiovllmbasexv3
type: application
version: 1.0.0
