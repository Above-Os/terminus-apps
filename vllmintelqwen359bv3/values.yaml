# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   vllmintelqwen359bv3 — engine Deployment (== appid / Release.Name)
#   llminit             — llm-init / entrance Deployment
workloads:
  llminit:
    replicaCount: 1
  vllmintelqwen359bv3:
    replicaCount: 1

# Intel XPU vLLM engine image (linux/amd64 only).
engine:
  images:
    intel: docker.io/intel/vllm:0.21.0-ubuntu24.04-20260805
