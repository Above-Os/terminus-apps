apiVersion: v1
kind: Service
metadata:
  name: genoffice
  namespace: '{{ .Release.Namespace }}'
  labels:
    io.kompose.service: genoffice
spec:
  ports:
    - name: http
      port: 3000
      targetPort: 3000
    - name: https
      port: 3001
      targetPort: 3001
  selector:
    io.kompose.service: genoffice
