apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (faster-whisper/CTranslate2 engine, llm-init audio-proxy). One instance = one CT2 whisper model; capability stt (fast under time-sliced vGPU). llm-init downloads the model AND fronts the engine's OpenAI /v1/* directly (ENGINE_KIND=audio, no nginx).
name: audiofasterwhisperbasexv3
type: application
version: 1.0.0
