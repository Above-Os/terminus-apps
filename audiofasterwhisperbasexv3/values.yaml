# Per-workload replica counts (keys match workloadReplicas in OlaresManifest): audiofasterwhisperbasexv3 = engine Deployment (== Release.Name), llminit = llm-init audio-proxy Deployment.
workloads:
  audiofasterwhisperbasexv3:
    replicaCount: 1
  llminit:
    replicaCount: 1
