# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   rssubscribe — rss-server + rssubscribe Deployment (must match metadata.name / appid)
workloads:
  rssubscribe:
    replicaCount: 1
