apiVersion: v1
kind: Service
metadata:
  name: note-svc
  namespace: {{ .Release.Namespace }}
  labels:
    app: note
spec:
  selector:
    app: note
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
