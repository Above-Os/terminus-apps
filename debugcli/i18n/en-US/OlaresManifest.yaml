metadata:
  title: Debug CLI
  description: Nginx index page and Ubuntu 22.04 web terminal.
spec:
  fullDescription: |
    Default opens a static index page (in-pod nginx). Use the Web Terminal entrance for a shell
    in the Ubuntu container. `/workspace` is under app data.
