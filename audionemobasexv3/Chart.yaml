apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (NVIDIA NeMo engine, llm-init audio-proxy). One instance = one streaming-diarization model; capability diar_stream (WebSocket). llm-init downloads the model AND fronts the engine's OpenAI /v1/* directly (ENGINE_KIND=audio, no nginx).
name: audionemobasexv3
type: application
version: 1.0.0
