# Per-workload replica counts (keys match workloadReplicas in OlaresManifest): audionemobasexv3 = engine Deployment (== Release.Name); llminit = llm-init audio-proxy Deployment (ENGINE_KIND=audio).
workloads:
  audionemobasexv3:
    replicaCount: 1
  llminit:
    replicaCount: 1
