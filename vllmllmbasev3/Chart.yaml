apiVersion: v2
appVersion: 0.29.0
description: "Generic vLLM + llm-init base; the model is supplied at install time via env. Fixed multi-node startup by scheduling llm-init and the inference engine on the same node to share local run-state."
name: vllmllmbasev3
type: application
version: 1.2.46