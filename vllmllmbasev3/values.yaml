# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas.deployments in OlaresManifest).
#   vllm          — engine Deployment
#   vllmllmbasev3 — llm-init / entrance Deployment
workloads:
  vllm:
    replicaCount: 1
  vllmllmbasev3:
    replicaCount: 1
