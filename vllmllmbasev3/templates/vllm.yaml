{{- /* vLLM engine Deployment. ENGINE_ARGS = raw vLLM CLI flags, word-split and
       appended after --model by wrappers/vllm.sh. */ -}}
{{- $oe := .Values.olaresEnv | default dict -}}
{{- $modelName := $oe.MODEL_NAME | default "" -}}
{{- $engineArgs := $oe.ENGINE_ARGS | default "" -}}
{{- /* MODEL_MODE=embedding -> auto-add vLLM's embedding flag so it works out of
       the box; skipped if the user already set a --task in ENGINE_ARGS. */ -}}
{{- $modelMode := $oe.MODEL_MODE | default "" -}}
{{- if and (eq $modelMode "embedding") (not (contains "--task" $engineArgs)) -}}
{{- $engineArgs = trim (printf "%s --task embed" $engineArgs) -}}
{{- end -}}
{{- /* Defaults avoid empty K8s quantities. CPU needs "m" (200m = 0.2 core).
       Sums (llm-init + vllm) must fit the manifest accelerator limits. */ -}}
{{- $cpuRequest := trim ($oe.VLLM_CPU_REQUEST | default "200m") -}}
{{- $memRequest := trim ($oe.VLLM_MEMORY_REQUEST | default "2Gi") -}}
{{- $cpuLimit := trim ($oe.VLLM_CPU_LIMIT | default "6") -}}
{{- $memLimit := trim ($oe.VLLM_MEMORY_LIMIT | default "35Gi") -}}
{{- /* nvidia.com/gpumem needs a BARE MiB int; Spark unified memory reuses the
       same value as pod memory ("<MiB>Mi"). Accepts 8Gi / 8192 / 8192Mi. */ -}}
{{- $gpuMiB := include "llmbase.gpuMiB" ($oe.VLLM_REQUIRED_GPU_MEMORY | default "4096") -}}
{{- $gpuType := .Values.gpu | default "" -}}
{{- if not $gpuType -}}
{{- $gpuType = .Values.GPU.Type | default "nvidia" -}}
{{- end -}}
{{- $unifiedMem := eq $gpuType "nvidia-gb10" -}}
{{- if $unifiedMem -}}
{{- $memRequest = printf "%sMi" $gpuMiB -}}
{{- $memLimit = printf "%sMi" $gpuMiB -}}
{{- end -}}
{{- /* Must match llm-init.yaml: per-release run-state hostPath. */ -}}
{{- $runStateHostPath := printf "%s/llm-init-run/%s" .Values.userspace.appCache .Release.Name -}}
---
# vLLM engine Deployment (GPU). wrappers/vllm.sh waits on llm-init's sentinel in
# the shared run-state dir, then serves --model "$MODEL_NAME" on :8000. llm-init
# reverse-proxies http://vllm:8000 via the "vllm" Service below.
# Shared hostPath volumes (read-only here, llm-init owns writes): hf-cache
# (/cache/hf/hub), run-state (sentinel handshake).
apiVersion: apps/v1
kind: Deployment
metadata:
  # Primary workload: named after the release (== appid vllmllmbasev3) for v3
  # allowMultipleInstall. Label/selector stays io.kompose.service: vllm.
  name: '{{ .Release.Name }}'
  namespace: {{ .Release.Namespace }}
  labels:
    io.kompose.service: vllm
  annotations:
    applications.app.bytetrade.io/gpu-inject: "vllm"
spec:
  replicas: {{ .Values.workloads.vllmllmbasev3.replicaCount }}
  selector:
    matchLabels:
      io.kompose.service: vllm
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        io.kompose.network/chrome-default: "true"
        io.kompose.service: vllm
    spec:
      restartPolicy: Always
      volumes:
        - name: hf-cache
          hostPath:
            path: "{{ .Values.userspace.appCommon }}/huggingface"
            type: DirectoryOrCreate
        - name: run-state
          hostPath:
            path: "{{ $runStateHostPath }}"
            type: DirectoryOrCreate
        - name: wrappers
          configMap:
            name: llm-init-wrappers
            defaultMode: 0555
      containers:
        - name: vllm
          image: docker.io/vllm/vllm-openai:v0.22.0-cu129
          imagePullPolicy: IfNotPresent
          command: ["/bin/sh", "/llm-init/wrappers/vllm.sh"]
          env:
            - name: ENGINE_KIND
              value: vllm
            - name: MODEL_NAME
              value: "{{ $modelName }}"
            - name: VLLM_PORT
              value: "8000"
            - name: WAIT_TIMEOUT
              value: "3600"
            - name: ENGINE_ARGS
              value: "{{ $engineArgs }}"
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: PGID
              value: "1000"
            - name: PUID
              value: "1000"
            - name: TZ
              value: Etc/UTC
          ports:
            - name: vllm
              containerPort: 8000
          # No readiness/liveness probe (intentional): serving readiness is owned
          # by llm-init (polls /v1/models, flips its own /readyz). A probe here
          # would keep the Pod NotReady during the long model load and couple the
          # app status back to it. restartPolicy: Always still recovers crashes.
          volumeMounts:
            - name: hf-cache
              mountPath: /cache/hf/hub
              readOnly: true
            - name: run-state
              mountPath: /run/llm-init
              readOnly: true
            - name: wrappers
              mountPath: /llm-init/wrappers
              readOnly: true
          resources:
            # nvidia.com/gpumem is the per-instance GPU memory quota read by the
            # Olares gpu-inject webhook (accelerator is -1/auto, can't carry it).
            # It's a non-overcommittable extended resource, so it must be in BOTH
            # requests and limits with the same value. Dropped on gb10 (unified
            # memory uses pod memory instead).
            requests:
              cpu: "{{ $cpuRequest }}"
              memory: "{{ $memRequest }}"
{{- if not $unifiedMem }}
              nvidia.com/gpumem: {{ $gpuMiB }}
{{- end }}
            limits:
              cpu: "{{ $cpuLimit }}"
              memory: "{{ $memLimit }}"
{{- if not $unifiedMem }}
              nvidia.com/gpumem: {{ $gpuMiB }}
{{- end }}
---
# Service name MUST be "vllm": llm-init v1.1.0 derives the engine URL
# http://vllm:8000 from ENGINE_KIND (no override). Internal only.
apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: {{ .Release.Namespace }}
  labels:
    io.kompose.service: vllm
spec:
  selector:
    io.kompose.service: vllm
  ports:
    - name: vllm
      port: 8000
      targetPort: 8000
  type: ClusterIP
