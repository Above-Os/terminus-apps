apiVersion: v1
kind: ConfigMap
metadata:
  name: clawdbot-sqlite-migration
  namespace: '{{ .Release.Namespace }}'
data:
  migrate-session-index.cjs: |
{{ .Files.Get "files/migrate-session-index.cjs" | indent 4 }}
