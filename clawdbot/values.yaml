# Intentionally empty overrides: Olares injects olaresEnv from OlaresManifest.yaml envs[].default at install.
olaresEnv: {}
workloads:
  clawdbot:
    replicaCount: 1
  clawdbotingress:
    replicaCount: 1
  terminal:
    replicaCount: 1
