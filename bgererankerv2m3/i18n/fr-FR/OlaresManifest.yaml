metadata:
  title: BGE-Reranker-v2-m3
  description: "Reranking cross-encoder BGE-Reranker-v2-m3 via rerank-server (OpenVINO / ONNX)"

spec:
  fullDescription: |
    **BGE-Reranker-v2-m3** service de reranking cross-encoder avec rerank-server. `POST /v1/rerank` compatible OpenAI via llm-init (port 8090).
