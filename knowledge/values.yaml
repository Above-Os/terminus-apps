# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   knowledge — knowledge backend Deployment
#               (knowledge + vectorcrawl + flaresolverr 3 containers)
workloads:
  knowledge:
    replicaCount: 1
