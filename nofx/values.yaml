# Image from leolianger/nofx: ./scripts/docker-build-push-goai007.sh
# Default script tag is `git describe` (e.g. commit hash). Either push with TAG=latest or set tag below to match push output.
image:
  repository: docker.io/goai007/nofx
  tag: "latest"
  pullPolicy: IfNotPresent

userspace:
  appData: ""
