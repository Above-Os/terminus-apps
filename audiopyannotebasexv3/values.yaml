# Per-workload replica counts (keys match workloadReplicas): audiopyannotebasexv3 = engine Deployment (== Release.Name), llminit = llm-init audio-proxy Deployment (ENGINE_KIND=audio).
workloads:
  audiopyannotebasexv3:
    replicaCount: 1
  llminit:
    replicaCount: 1
