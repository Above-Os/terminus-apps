apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (pyannote/torch engine, llm-init audio-proxy). One instance = one model; capabilities via MODEL_SUPPORTS (vad / diar / speaker_embed / enhance). llm-init downloads the model AND fronts the engine's OpenAI /v1/* directly (ENGINE_KIND=audio, no nginx).
name: audiopyannotebasexv3
type: application
version: 1.0.0
