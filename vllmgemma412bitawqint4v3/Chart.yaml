apiVersion: v2
appVersion: 0.24.0
description: "Gemma-4-12B (AWQ-INT4) served with vLLM + llm-init (GPU only). Fixed multi-node startup by scheduling llm-init and the inference engine on the same node to share local run-state."
name: vllmgemma412bitawqint4v3
type: application
version: 1.0.25