apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (Whisper family on CTranslate2 — faster-whisper engine, llm-init reverse-proxy). One instance = one model; capability via MODEL_SUPPORTS (stt). llm-init downloads the model AND reverse-proxies the engine's OpenAI /v1/*.
name: audiofasterwhisperbasev3
type: application
version: 1.0.0
