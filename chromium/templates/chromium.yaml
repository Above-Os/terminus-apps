---
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   creationTimestamp: null
#   labels:
#     io.kompose.service: chromium-claim0
#   name: chromium-claim0
#   namespace: {{ .Release.Namespace }}
# spec:
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: 100Mi
# status: {}
# ---
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.28.0 (c4137012e)
  creationTimestamp: null
  labels:
    io.kompose.service: chromium
  name: chromium
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: chromium
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.28.0 (c4137012e)
      creationTimestamp: null
      labels:
        io.kompose.network/chrome-default: "true"
        io.kompose.service: chromium
    spec:
      containers:
        - env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: Etc/UTC
            - name: DeviceName
              value: {{ .Values.deviceName }}
        {{- if and .Values.deviceName (eq .Values.deviceName "Olares One") }}
            # Olares One: Selkies/Wayland 版镜像，启用 Intel iGPU VAAPI 硬件编码
            - name: CHROME_CLI
              value: '--ignore-gpu-blocklist --enable-gpu-rasterization --enable-zero-copy --use-gl=egl --enable-features=UseOzonePlatform,VaapiVideoDecoder,VaapiVideoEncoder,CanvasOopRasterization,ReduceAcceptLanguageHeader'
            - name: DRINODE
              value: /dev/dri/renderD128
            - name: DRI_NODE
              value: /dev/dri/renderD128
            - name: LIBVA_DRIVER_NAME
              value: iHD
        {{- else }}
            # 非 Olares One（ARM / Selfhosted / NVIDIA 等）: 沿用旧 KasmVNC 版镜像
            # KasmVNC 走纯软件 VNC framebuffer，无需 GPU、可在任意架构稳定运行
            - name: CHROME_CLI
              value: https://www.linuxserver.io/
            - name: CHROME_FLAGS
              value: '--enable-features=ReduceAcceptLanguageHeader'
        {{- end }}
        {{- if and .Values.deviceName (eq .Values.deviceName "Olares One") }}
          image: "docker.io/beclab/harveyff-chromium:1e736296-ls34"
        {{- else }}
          image: "docker.io/beclab/aboveos-kldtks-chromium:version-e3efdad3-cn"
        {{- end }}
          name: chromium
          securityContext:
            privileged: true
          resources:
            requests:
              cpu: "500m"
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 6Gi
          ports:
            - containerPort: 3000
            - containerPort: 3001
            - containerPort: 8082
          volumeMounts:
            - mountPath: /config
              name: config
        {{- if and .Values.deviceName (eq .Values.deviceName "Olares One") }}
            - mountPath: /dev/dri
              name: dev-dri
        {{- end }}
      restartPolicy: Always
      volumes:
        - name: config
          hostPath:
            type: DirectoryOrCreate
            {{- if .Values.sysVersion }}
              {{- if semverCompare ">=1.12.3-0" (toString .Values.sysVersion) }}
            path: {{ .Values.userspace.appCache }}/config
              {{- else }}
            path: {{ .Values.userspace.appCache }}/chromium/config
              {{- end }}
            {{- else }}
            path: {{ .Values.userspace.appCache }}/chromium/config
            {{- end }}
    {{- if and .Values.deviceName (eq .Values.deviceName "Olares One") }}
        - name: dev-dri
          hostPath:
            path: /dev/dri
    {{- end }}
status: {}
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.28.0 (c4137012e)
  creationTimestamp: null
  labels:
    io.kompose.service: chromium
  name: chromium
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
    - name: "3001"
      port: 3001
      targetPort: 3001
      nodePort: 30001
    - name: "8082"
      port: 8082
      targetPort: 8082
  selector:
    io.kompose.service: chromium
status:
  loadBalancer: {}
