apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (NVIDIA NeMo — Streaming Sortformer, llm-init reverse-proxy). One instance = one model; capability via MODEL_SUPPORTS (diar_stream). llm-init downloads the model AND reverse-proxies the engine's OpenAI /v1/*.
name: audionemobasev3
type: application
version: 1.0.0
