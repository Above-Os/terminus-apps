# Per-workload replica counts (keys match workloadReplicas in OlaresManifest).
#   audiolabx2v3 — engine Deployment (== Release.Name)
#   llminit      — llm-init audio-proxy Deployment (ENGINE_KIND=audio)
workloads:
  audiolabx2v3:
    replicaCount: 1
  llminit:
    replicaCount: 1
