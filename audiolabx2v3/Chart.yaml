apiVersion: v2
appVersion: "1.0.0"
description: Generic audio-model base (llm-init audio-proxy variant). llm-init downloads the model AND fronts the engine's OpenAI-compatible /v1/* directly (ENGINE_KIND=audio, no nginx). Model + mode supplied at install via env.
name: audiolabx2v3
type: application
version: 1.0.0
