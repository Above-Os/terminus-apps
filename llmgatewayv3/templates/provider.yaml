apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: backend:{{ .Release.Namespace }}:llmgateway-provider
  annotations:
    provider-registry-ref: "{{ .Release.Namespace }}/llmgateway-provider"
    provider-service-ref: "llmgateway-provider.{{ .Release.Namespace }}:28080"
rules:
  - nonResourceURLs: ["*"]
    verbs: ["*"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: backend:sa:default:llmgateway
rules:
  - apiGroups:
      - "*"
    resources:
      - users
    verbs:
      - list
      - watch
      - get

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: backend:sa:default:llmgateway
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: backend:sa:default:llmgateway
subjects:
  - kind: ServiceAccount
    name: default
    namespace: "{{ .Release.Namespace }}"
