apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (Qwen ASR family — qwen-asr in-process vLLM engine, llm-init reverse-proxy). One instance = one model; capabilities via MODEL_SUPPORTS (stt / stt_stream / align). llm-init downloads the model AND reverse-proxies the engine's OpenAI /v1/*.
name: audioqwenbasev3
type: application
version: 1.0.0
