# Per-workload replica counts (keys match Deployment metadata.name and
# workloadReplicas in OlaresManifest).
#   llamacpp          — engine Deployment
#   llamacppllmbasev3 — llm-init / entrance Deployment
workloads:
  llamacpp:
    replicaCount: 1
  llamacppllmbasev3:
    replicaCount: 1
