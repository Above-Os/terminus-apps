apiVersion: v2
appVersion: b10752
description: "Generic llama.cpp + llm-init base; the GGUF model is supplied at install time via env. Fixed multi-node startup by scheduling llm-init and the inference engine on the same node to share local run-state."
name: llamacppllmbasev3
type: application
version: 1.2.49