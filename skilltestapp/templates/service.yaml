apiVersion: v1
kind: Service
metadata:
  name: {{ include "skilltestapp.fullname" . }}
  labels:
    {{- include "skilltestapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skilltestapp.selectorLabels" . | nindent 4 }}
