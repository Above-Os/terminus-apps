apiVersion: v2
appVersion: "1.0.0"
description: Audio model base (pyannote / torch / speechbrain, llm-init reverse-proxy). One instance = one model; capability via MODEL_SUPPORTS (vad / diar / speaker_embed / enhance). llm-init downloads the model AND reverse-proxies the engine's OpenAI /v1/*.
name: audiopyannotebasev3
type: application
version: 1.0.0
