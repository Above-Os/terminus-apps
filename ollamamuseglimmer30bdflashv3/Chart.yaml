apiVersion: v2
appVersion: 0.32.8-rc0
description: "Muse Glimmer 30B Q4_K_M + DFlash served with Ollama + llm-init (GPU only). Fixed multi-node startup by scheduling llm-init and the inference engine on the same node to share local run-state."
name: ollamamuseglimmer30bdflashv3
type: application
version: 1.0.14